# Production build env (picked up by `vite build`). Dev uses .env (localhost).
# Live backend base — see api.organikaly.com (CONTRACT §10 / STORE_CONTRACT §15.1).
VITE_API_BASE=https://api.organikaly.com/api/v1
VITE_STORE_ENABLED=true
